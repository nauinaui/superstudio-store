define(['./Base.js', 'bootstrap'], function (Base, Bootstrap) {
    var mConfirmation = new Base('data for Page Confirmation loaded');

	/**
	 * =================
	 * FUNCTIONS
	 * =================
	 */


	/**
	 * =================
	 * EVENTS
	 * =================
	 */
	

	/**
	 * =================
	 * TO EXECUTE WHEN INIT
	 * =================
	 */

    $(document).ready( function() {



    });

    return mConfirmation;
});